<div class="row">
    <?php
    $host = $this->domainLink(1, true);
    echo $this->notification($this->service);
    if ($this->activated == true) {
        echo __('nowYouCan') . ' <a href = "' . $host . 'sign-in">' .
            __('login') . '</a>';
    }
    ?>
</div>