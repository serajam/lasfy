<div class="row">
    <?php

    $i = 1;
    foreach ($this->cities as $city) {
        $link = $this->domainLink(1, true) . 'tag/' . mb_strtolower($city['tagName']);
        echo '<div class="medium-2 columns city-tag"><a href="' . $link . '">' . $city['tagName'] . '</a></div>';
    }
    ?>
</div>
